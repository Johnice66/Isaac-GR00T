# GR00T N1.7 常见问题

本文件对应英文版 [FAQ.md](FAQ.md)，聚焦当前仓库里最常被问到的基础问题。

## 基础设施与硬件

### 数据加载器是否使用 GPU 加速？

不是。当前数据加载仍然是 CPU 路线，但已经针对多模态数据做过优化，目标是避免成为训练瓶颈。

### 预训练和后训练是否共用同一套数据加载管线？

是。当前仓库的训练数据加载逻辑是一套统一实现。

### 部署图里的 Policy Remote Server 是做什么的？

它把模型推理和机器人本体解耦开。这样可以把推理放到算力更强的 GPU 服务器上，而机器人侧只保留轻量控制逻辑。

## 模型与训练

### 新 embodiment 或新任务大概需要多少数据？

- 固定场景、简单抓放：约 100 条轨迹起步
- 多步骤或复杂场景：约 500 条以上
- 高自由度 humanoid 任务：通常需要 2000 条以上
- 精细操作：100 到 500 条常见，但最好有更强的先验或更高质量演示

### 如何提高困难任务成功率？

推荐先用一批较干净的 teleop 数据训练一个初版策略，然后做 HG-DAgger 式迭代采集：运行策略，失败时人工接管，把修正后的轨迹继续加回数据集。

### 只关心一台机器人时，引入别的机器人真机数据还有用吗？

通常有用。跨 embodiment 的真实数据能增强视觉多样性和鲁棒性，对目标机器人也可能有帮助。

## 模型能力边界

### 光照变化、颜色变化会不会明显掉点？

会。文档建议在训练中启用 color jitter，并在不同光照条件下补 20 到 50 条真实数据，减少过拟合。

### GR00T N1.x 能不能做通用 VQA 或复杂推理？

不是当前重点。N1.x 更偏动作生成，不是开放式问答模型。

### 模型会不会自动学会“失败后重试”？

不要默认会。当前架构是无状态的，恢复行为更适合通过 DAgger 或 RL 专门补出来。
